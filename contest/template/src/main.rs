#![allow(unused_imports)]
use std::{*, collections::*, ops::*, cmp::*, iter::*};
use proconio::{input, fastout};
use common::*;

fn main() {
    solve();
}

// CONTEST(abcXXX-a)
#[fastout]
fn solve() {
}

// #CAP(fumin::modint)
pub mod fumin {
}

pub mod common {
// CAP(common)
}