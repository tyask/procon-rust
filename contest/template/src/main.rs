#![allow(unused_imports)]
use std::{*, collections::*, ops::*, cmp::*, iter::*};
use proconio::{input, fastout};
use common::*;

fn main() {
    solve();
}

#[fastout]
fn solve() {
}

pub mod ex {
}

pub mod common {
// CAP(common)
}