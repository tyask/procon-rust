#!/bin/bash

alias cnew="$(dirname ${BASH_SOURCE[0]})/compete_new.sh"
alias ctest='cargo compete test'
alias csub='cargo compete submit --no-watch'
alias copen='cargo compete open'
alias r="$(dirname ${BASH_SOURCE[0]})/cargo_run.sh"

